rules:
  block_on_alerts:
    severities: [medium, high]
    lookback_hours: 24

  require_baselines:
    features: ["age_bucket"]

  block_high_alerts:
    enabled: true

  block_slo_alerts:
    enabled: true
    min_severity: medium

  require_clean_slo:
    enabled: false

